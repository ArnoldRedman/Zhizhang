# 织章写作链路交接（2026-09-23）

> 给接手 AI：先读本文件及当前仓库，再动代码。用户要的是懒人连续创作：应用自己写、有限次数修正和复审，合格后接着写到结局；不能让用户每章手动救火，也不能把不合格稿伪装成通过。

## 仓库与数据边界

- 仓库：`D:\ApiSaverWriter`，`main` 已推送到 `origin/main`，最新提交 `0bc3483`（前一笔 `995abfa`）。先 `git pull origin main`，检查 `git status`。`0bc3483` 是明确的 WIP 接手点，不是质量验收完成的声明。
- 小说《穿成恶人前夫后，我只想安静等死》的正文、章节历史、章纲和文风保存在**应用本机数据**，不在 Git 仓库。原机器路径为 `%APPDATA%\com.zhizhang.app\projects\穿成恶人前夫后，我只想安静等死`；另一台电脑未必有这份数据。拿不到原始资料时不要编造试写结论，也不要把整本小说或 API Key 提交进仓库。
- 用户有意设置过部分写作约束。不要把所有文风和规划要求都判为错误、擅自解绑文风或覆盖本地原件。不要批量重写整本书，不要直接覆盖第 132～134 章。现在有一份草稿停在“接受并写入”的确认框；先选**取消**以保住旧章，重启窗口前尽量保全草稿。

## 现在发生了什么

用户在开发版点击“接受并写入”时，旧版原生 `window.confirm` 弹出不可复制的 `localhost:5173 显示`。通过 Tauri WebView 的 Windows UI Automation 读出的内容是：

- 正文 **1897 字**，项目目标 **2200～2640 字**。
- 审查指出严冬青本章已出庭就母本印鉴作证，却又被宣布为“明日第一位专家证人”；第 131 章他也已就荧光、印文、印泥作证，可能与第 133 章专家证言重演。
- 一致性审查未完成：`deepseek/deepseek-flash`，OpenAI-compatible 接口，`max_tokens=16000`，`finish_reason=length`，正文长度 0，推理长度 46857，工具调用数 0。

**不要点击确认覆盖原章。** 当前原稿 132～134 本身也互相矛盾：132 已宣判，133 又回到质证和择期宣判，134 再宣判。后文以 134 章终审胜诉为既成事实。这个问题不能靠换几个“反 AI 味”词语解决。

## 当前代码及尚未解决的点

- `packages/contracts/src/chapter-acceptance.ts`：前后端共用最终稿验收，检查字数、审查是否完成、重复事件和重要事实冲突。`desktop-app/src/features/chapter-agent/acceptance.ts` 转出该函数。
- `sidecars/agent-runtime/src/graphs/chapter-write.graph.ts`：不合格稿最多自动修一次，修正版重新过验证门和审查；若修不净才停草稿，不得把 `advances` 伪造为 true。刚补了“部分审查视角失败，但已知字数/事实问题时仍可自动修一次”的分支。检查其成本、失败边界和最后一次审查与正文是否同版。
- `sidecars/agent-runtime/src/application/chapter-review.ts`、`src/main.ts`：审查直接带紧邻上一章正文，防止摘要漏掉宣判或证人已出庭等不可逆事实。
- `desktop-app/src/App.tsx`、`App.css`：原生 `window.confirm` 已改为应用内可选中、可复制问题的确认弹窗，覆盖按钮需要二次明确操作。**尚未通过真实 Tauri 窗口实测该新弹窗**；旧原生弹窗仍可能停在用户的旧进程内，修改代码不会自动关闭它。
- `sidecars/agent-runtime/src/models/model-api.ts` 与 `src/application/review-runner.ts`：真实模型的推理耗尽但无正文仍未解决。不要简单无限增加 token、跳过审查或无限重试。查实际网关/模型的推理参数兼容性、审查输入大小和可控的降级方式；记录每次调用次数与成本，避免“懒人”变成高价循环。
- 上述提交后运行 `npm test` 通过（真实 API E2E 因 `API_SAVER_KEY` 未设置而跳过）；`npm run typecheck` 在修正确认框的项目 ID 类型后通过。**该最后版本尚未重新做桌面构建和 Tauri 实机交互验证。** 不要用单元测试通过冒充小说文风已好。
- 当前开发服务器 `localhost:5173` 曾多次退出；进入新机器要重新启动、核实 Tauri 实际加载的是最新 `sidecars/agent-runtime/dist/main.js`，而非旧打包 `main.cjs`。此前用户要求删除测试生成的 `.pi` 日志，不要无意把生成日志加入提交。

## 接手顺序

1. 保全旧章和当前草稿，确认界面不再出现不可复制的原生确认框；在 Tauri 窗口实测可复制问题、取消、二次覆盖及切换项目/章节后的防误写。不要代用户按“仍要覆盖”。
2. 用最小可复现请求检查 DeepSeek Flash 审查为何只输出推理、无 JSON 正文；针对模型/网关失败给出**有界且可观察**的处理，不把失败当 PASS。优先定位具体阶段及请求大小，再决定模型参数或审查降级。
3. 审阅一次自动修正和复审链：只在最终稿字数、事件顺序和审查都合格时连续写下一章；失败保留草稿与原章并给出明确原因。跑 `npm test`、`npm run typecheck`、`npm --prefix desktop-app run build`、`npm --prefix sidecars/agent-runtime run build`；改 Rust 时再跑 Rust 测试。最后 `git diff --check`。
4. 不要为了通过检查把项目本来有意设置的约束一删了之。要区分模型能力、过量提示词、上下文事实污染和采用流程；用同一章、同一事实材料做有记录的对照。

## 第 132～134 章试写验收（请亲自读稿，别只信审查分数）

先取得原书的第 **131～135** 章、对应章纲/记忆/人物状态、当前绑定文风与章节历史；检查后文引用，保留原件与快照。**先独立阅读并引用原文证据做诊断**，不要只复述本文件的结论：列出每章在什么时间、谁作证、哪些证据已被采纳、是否宣判、人物当下知道什么；标明 132→133→134 的不可逆结果是否被重演或退回待定。

然后在**项目正文之外**写三份候选草稿（例如本机临时目录），依次考虑：132 聚焦信托证据与抗辩但不提前宣判；133 处理尚未完成的专家证言，不让第 131/132 章已作证的人以同一内容再次登场，也不提前判决；134 是唯一一次终审宣判和后果。若原始章纲或第 135 章之后的事实与此安排冲突，先指出冲突并提出最小调整，不要硬写。保留作者认定的硬事实，不新增关键裁决或角色设定。

试写的目的不仅是“字数过门”，还要看**人是不是人**：对话各有目的、会回应对方刚说的话，情绪能从处境和选择读出来；不是全员点头、沉默、摆弄笔帽和纸角。不要再加整页禁词表或为了过审强塞摸手/物件动作。按应用计数方式检查每章目标 2200 字、上限 2640 字，但不许重复证词和动作凑字。三章连读后与用户认为尚可的早期章节比较，给出具体引文与判断：故事顺序是否顺、人物声音是否分得开、关系发展是否可信、是否仍有机械 AI 味。

可少量调用应用写作 API 做对照，但要记录模型、模式、输入资料、调用与输出；真实调用有成本，避免批量试错。交付内容应包括：独立问题报告、三份候选稿、与旧稿逐章差异、自动流程哪里仍会失败。**除非用户审阅认可，不能把候选稿自动写进现有小说，也不能据三章试写擅自重写第 135～204 章。**
