# TODO

只记还没做完的事。做完的直接删掉，根因与修法写进 commit message 和代码注释，不在这里堆。

## 写作流程

- [ ] **真机验证 2026-09-20 的写作流程改造**：章节图改成“构思 → 正文（纯文本、温度 0.85）→ 本地验证门（标点归一、blocking 句式定向修订一次）→ 三档审查（full / lean / solo，只出报告；判定重复前文才换事重写一次，带证据的 S1 事实矛盾才定点修订一次）”，系统提示词砍到三行，去掉自动技能匹配和字数调整；卡片近乎整张带入、世界观文档不再 6000 字节头尾截断、总纲逐章条目只保留本章那条、账本只列埋了三章以上的长线伏笔、记忆新增 relationshipState、模型的疑问走「【给作者】」进“给作者｜待答”文档；记忆新增 readerKnown / authorTruth / nextChapterPromise / newlyIntroduced，账本带上一章承诺与作者真相；写作时带最近五章开头结尾句；项目设置新增审查档位、引号风格、允许句式，“全书统一标点”一键归一；写作操作台新增“重写旧章”（保事件换写法 / 从构思重来，重来时先删旧章纲）；拆书新增“全书聚合”（文风档案、情绪模块、节奏表、原文锚点），绑定后构思与正文按基调召回；正文批注定点修订；卡片常驻开关（常驻卡进构思，正文只带构思点到名的卡）；作品默认技能加按章纲标签自动路由；连续创作每十章刷新全部卡片状态；AI 率门（本地启发式，缺省 30%，超了只对疑似段落去一次 AI 味）。全部只过了单测，要用《穿成恶人前夫后》从第 205 章起连写两三章看：人物有没有按沈妄卡（2026-09-20 新写的初稿）说话、感情线有没有承接、审查 advances 误判率、纯文本输出的标题行解析在各家中转是否稳定。总纲里“第177章《一滴纸浆的尺度》已经完成”一句与实际进度不符，作者需改掉。

- [ ] **总纲对 178 章以后只写了“不预设具体结果”**：已在《穿成恶人前夫后》总纲里改为“七、第179章起分阶段规划”（179～185 / 186～195 / 196～205 / 206～250 四段）。其他小说同样需要按卷间写明的区间补分阶段规划，否则模型没有可推进的目标节点。
- [ ] **运行时"混合检索"检索的是提示词里已有的内容**：`chapter.write` 每次请求新建内存库，只装传入的记忆与文档再在里面搜。按项目持久化 StoryStore 仍待做。
- [ ] **换模型会静默换掉会话**：会话 key 里带 `model` 与 `apiMode`，换模型等于开新对话；若希望跨模型沿用，把这两个字段从 `sessionKey` 里去掉。
- [ ] **旧章批量审查只过了单测**：`chapter.review`（现在也先过本地验证门、按档位跑多视角，报告里带 S1～S4 与验证门条目）、章节面板的写作操作台“审查旧章”（并发池 `utils/concurrency.ts`）、“批量修订选中章 + 统一口径”（S4 读感提示不进修订指令）、跨章同源问题聚类，报告落盘为大纲文档 `审查报告｜第 N 章`。批量修订仍走 `text.transform` 的 revise 模式。要验证审查 JSON 解析、并发下的限流阈值、lean 档每章两次调用的耗时与花费。
- [ ] **《穿成恶人前夫后》第 140 章起的旧章处理**：2026-09-20 实测 140～149 章全部报时式开头、抒情收尾；174～204 章 31 章里 18 章收在“灯”或“没动、没关、没再说”，多章停在同一件事；引号风格全书漂了 8 次。计划：先“全书统一标点”，140～149 章用“保事件换写法”重写，150～173 章抽查，174～204 章用“从构思重来”。先拿 140、147、192、203 四章试重写，读过再定全量；模型仍用 deepseek，试写不理想再换 gpt 系列对比。
- [ ] **跨章会话交接被压到 2.2KB**：`chapter-write.graph.ts` 的 `splitSessionContext` 整段会话 compactText 到 2200 字节，多章交接实际不起作用。
- [ ] **结构化小任务没有按次降低推理强度的开关**：计划、审查、补标题这类几百字 JSON 的调用用的是全局推理强度，上限一紧就返回空内容。`ChatOptions` 增加按次的推理强度覆盖，这几类小任务固定用最低档。
- [ ] **作者可手改的故事进度板**：阶段节拍表覆盖了“接下来 N 章各写什么”；“已完成节点、进行中伏笔”这张表仍靠运行时每次从记忆临时生成，作者改不了。`domain/project.ts` 里的 `OutlineNode` 树是死代码，正是这张表需要的结构。
- [ ] **移动端 platform.ts 另有一套 chapter.write 提示词**：直接把参数 JSON 塞给模型，与运行时的章节图不共享任何改动。

## 性能

- [ ] **metadata.json 过大**：《穿成恶人前夫后》4.2MB，其中 1.6MB 是 AI 检测按段落存的全文副本、0.3MB 是章节快照，每次自动保存都要序列化并经 IPC 传一次。细粒度 `save_chapter` 命令能把每次保存从整个项目数组降到一章。
- [ ] **图谱长尾噪声**：781 个实体里 223 个只连一条边，另有“天宇法务 / 天宇法务部 / 天宇法务天团”这类同实体变体，来自记忆提炼提示词本身；缺一个清理与合并入口。

## 架构与工程质量

- [ ] **前端 App.tsx 是 7500 多行的单体组件**：首页、编辑器、书库、拆书、扫榜、技能、文风、设置、记忆中心、图谱全在一个函数里。纯逻辑已陆续抽到 `domain/` 与 `features/*`（记忆、章号与章纲意图、AI 检测、章节智能体入参、图谱布局与索引、卡片状态、图谱合并）；仍待做：项目 Agent 变更落地（`applyProjectAgentChangeBatch` 一族）、书库/拆书/扫榜的数据流、备份与同步，然后按页面拆组件。
- [ ] **运行时 main.ts 把五个核心 handler 内联在一个函数里**：memory.write、project.agent.chat、card.write、outline.write、chapter.write 都在 `handleLegacyRequest` 里。按 rpc/chapter-handlers.ts、outline-handlers.ts、card-handlers.ts、memory-handlers.ts、project-agent-handlers.ts 注册到 RpcRegistry，main.ts 只剩组合根。
- [ ] **platform.ts 1100 行把移动端整套智能体重写了一遍**：模型客户端、拆章、补标题、项目 Agent 循环、各类提示词都有第二份实现，靠注释人工同步。
- [ ] **Rust lib.rs 1049 行混了六类职责**：百度网盘、本地备份与导出、Agent 对话存储、目录打开、系统代理探测、Tauri 注册都在一个文件里。
- [ ] **组件层零测试**：前端测试全部落在 domain/utils/features，App.tsx 本身没有测试，要等页面拆出来之后才有下手处。
- [ ] **TS 大版本与 lockfile 不统一**：desktop-app 锁 TS 6.0，其余包 5.x，两套 lockfile；oxlint 只启用两条规则，exhaustive-deps 有 2 处告警。

## 待确认

- [ ] **同一章有多份章纲时绑定取先出现的那份**：`boundChapterOutlineFor` 用 `find`，按标题章号匹配会命中旧的那份（第 178 章有“章纲｜第 178 章”与“章纲｜第 178 章 失败样上桌”两份，绑定到前一份）。

- [ ] **`reasoning_effort` 在目标中转站是否真实生效**：请求已发送该字段，`max` 降级为 `high`；503 兼容重试会移除该字段，400 不会。只有中转站出现 400 或确认忽略该字段时再扩展重试规则。
- [ ] **同名大纲共用一个 Markdown 文件**：文件按标题命名（`大纲/<标题>.md`），两份同名大纲（如同一章的旧版与新版章纲）会互相覆盖，删重复条目时也会连带删掉另一份的正文。2026-09-16 手动清理《穿成恶人前夫后》第177章两份同名章纲时真的删掉过一次。建议标题唯一化（保存时撞名就自动加序号）或文件按 id 命名。
- [ ] **账本超出单份 6000 字节预算**：`worldSetting` 每份文档 `compactText(content, 6000)`，而《穿成恶人前夫后》的《动态状态与硬事实账本》有 11.4KB，中段 `marital_status` 与 `relationship_stages` 被截断，模型看不到人物关系阶段。已把逐章天数表压成节点表，但彻底解决要拆成两份文档（预算按份算）或给账本单独提预算。
- [ ] **导入格式与导入记忆**：`desktop-app/scripts/import-story-folder.mjs` 只适配 StoryForge 目录结构，且导入后 `memories` 为空。有真实样本再加映射；记忆可在应用里用“从第 N 章开始补章节记忆”补齐。
